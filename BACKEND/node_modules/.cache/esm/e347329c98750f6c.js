let express,mongoose,bodyParser,cors,dotenv;_987‍.w("express",[["default",["express"],function(v){express=v}]]);_987‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_987‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_987‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_987‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);// Import necessary modules






// Load environment variables from .env file
dotenv.config();

// Create an Express app
const app = express();

// Set up middleware
app.use(cors());
app.use(bodyParser.json());

// Set up MongoDB connection
const URL = process.env.MONGODB_URL;

mongoose.connect(URL, {
  useCreateIndex: true,
  useNewUrlParser: true,
  useUnifiedTopology: true,
  useFindAndModify: false
});

const connection = mongoose.connection;

connection.once('open', () => {
  console.log('MongoDB connection success!');
});

// Set up server to listen on the specified port
const PORT = process.env.PORT || 8070;

app.listen(PORT, () => {
  console.log(`Server is up and running on port number: ${PORT}`);
});
